

 // TODO создать необходимые методы(функции) для анализа достижений
 // смотрите на функцию которая появиться в программном коде и сделайте на подобии
 // Необходимо реализовать
 // 1  вывод на экран рейтинга достижения одного человека
 // 2 вывод на экран сравнительный список достижений в виде "ФИО" "Количество баллов набрано" "Количество баллов необходимое" "Формулировка цели"
 // 3 вывод на экран списка целей
 
 using System;

public class Rating
{
// это конструктор класса - метод(функция ) который отыечает за создание класса
Модификаторы дотупа
1 private - закрыт доступ из других модулей
2 public - для доступа из других модулей
	
	public Rating()
	{
	 // здесь можно писать код программы которая будет выполнятся только при создании объекта
	}
	
	//метод вывода на экран рейтинга достижения одного человека
	public void PrintTargetOne(int iduser, <здесь будет использоваться то, что сделает Артур:)>){
	  
	  Console.WriteLine(<здесь будет использоваться, то что сделает Артур:)>.GetMaxRating(iduser));
	} 
	
}
